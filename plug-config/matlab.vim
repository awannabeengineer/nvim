let g:matlab_auto_mappings = 0  "automatic mappings disabled 
let g:matlab_server_launcher = 'vim'   "launch the server in a Neovim terminal buffer can use tmux
let g:matlab_server_split = 'vertical'    "launch the server in a vertical split can use horizonatal

